{
  "pluginAlias": "X-Sense",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Homebridge X-Sense"
      },
      "username": {
        "title": "Email",
        "type": "string",
        "description": "Your X-Sense account email address."
      },
      "password": {
        "title": "Password",
        "type": "string",
        "description": "Your X-Sense account password."
      },
      "pollingInterval": {
        "title": "Polling Interval (minutes)",
        "type": "integer",
        "default": 15,
        "minimum": 5,
        "description": "How often to poll for device status updates (in minutes)."
      }
    },
    "required": ["username", "password"]
  }
}
